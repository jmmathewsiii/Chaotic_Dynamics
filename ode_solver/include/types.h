#ifndef TYPES_H
#define TYPES_H

#include <iostream>
#include <vector>
#include <string>
#include "state.h"

using std::string;
using std::vector;
using std::cout;
using std::cerr;

using VS = vector<State>;
using VSS = vector<vector<State>>;

#endif
